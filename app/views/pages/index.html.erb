<link href='https://fonts.googleapis.com/css?family=Allerta Stencil' rel='stylesheet'>

<% if user_signed_in? %>
  <h1>Hello <%= current_user.username %>!</h1>
<% else %>
  <li>
    <%= link_to('Login', new_user_session_path) %>
  </li>
<% end %>

<%= javascript_pack_tag 'application' %>
<%= react_component("ParentComponent", { recommendations:@recommendations, wishlists:@wishlists, user:current_user, current_user:current_user }) %>

<!-- <button href='http://localhost:3000/friendships' </button> -->

<% if user_signed_in? %>
  <%= link_to "Friends", friendships_path %>
<% end %>
